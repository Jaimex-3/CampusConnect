<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="Campus Connect"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.CampusConnect"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Launcher Activity -->
        <activity android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.ScheduleActivity" />
        <activity android:name=".ui.MentorsListActivity" />
        <activity android:name=".ui.ChatActivity" />
        <activity android:name=".ui.MapActivity" />
        <activity android:name=".ui.ResourcesFaqActivity" />
        <activity android:name=".ui.EventDetailsActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDlPYaAy1vnBc6h55c_jVNsA46rfXw_Qbw" />

    </application>

</manifest>